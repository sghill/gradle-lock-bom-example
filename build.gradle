plugins {
  id 'java'
}

repositories {
  jcenter()
}

dependencies {
  implementation 'com.github.rholder:guava-retrying:latest.release'
  implementation 'com.google.guava:guava:latest.release'
}

dependencyLocking {
  lockAllConfigurations()
}

